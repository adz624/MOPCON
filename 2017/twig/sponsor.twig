{% extends 'main.twig' %} {% block content %}
<!-- header	 -->
<section class="pageHead">
    <img src="images/headPage-sponsor.svg" alt="贊助標題圖片">
</section>
<!-- sponsor -->
<section class="sponsor container">

    <!-- 等級一 -->
    
    <div class="sponsor_content">
        <div class="sponsor_content_title setCenter">
            Tony Stark
        </div>
        <div class="content-radius">
            <div class="row">
            {% for id, data in sponsors %}
            {% if data['type'] == 'Tony Stark' %}
                <div class="pic-item col-lg-12">
                    <a href="{{ data['website'] }}">
						<img src="./images/sponsor/{{ data['logo'] }}" alt="">
					</a>
                </div>
            {% endif %}
            {% endfor%}
            </div>
        </div>
    </div>
    <!-- 等級二 -->
    <div class="sponsor_content">
        <div class="sponsor_content_title setCenter">
            Bruce Wayne
        </div>
        <div class="content-radius">
            <div class="row">
            {% for id, data in sponsors %}
            {% if data['type'] == 'Bruce Wayne' %}
                <div class="pic-item col-lg-6 col-md-6 col-sm-6 col-xs-6">
                <a href="{{ data['website'] }}">
						<img src="./images/sponsor/{{ data['logo'] }}" alt="">
				</a>
                </div>
            {% endif %}
            {% endfor%}
            </div>
        </div>
    </div>
    <!-- 等級三 -->
    
    
    <div class="sponsor_content">
        <div class="sponsor_content_title setCenter">
            Hacker
        </div>
        <div class="content-radius">
            <div class="row">
            {% for id, data in sponsors %}
                {% if data['type'] == 'Geek' %}
                <div class="pic-item col-lg-4 col-md-4 col-sm-4 col-xs-6">
                    <a href="{{ data['website'] }}">
						<img src="./images/sponsor/{{ data['logo'] }}" alt="">
					</a>
                </div>
                
                {% endif %}
            {% endfor%}
            </div>
        </div>
    </div>
    
    
    <div class="sponsor_content">
        <div class="sponso r_content_title setCenter">
            Developer
        </div>
        <div class="content-radius">
            <div class="row">
            {% for id, data in sponsors %}
            {% if data['type'] == 'Developer' %}
            {% if data['type'] == 'Developer' %}
                <div class="pic-item col-lg-4 col-md-4 col-sm-4 col-xs-6">
                    <a href="{{ data['website'] }}">
						<img src="./images/sponsor/{{ data['logo'] }}" alt="">
					</a>
                </div>
               
            {% endif %}
            {% endfor%}
            </div>
        </div>
    </div>
    
    <div class="sponsor_content">
        <div class="sponsor_content_title setCenter">
            特別感謝
        </div>
        <div class="content-radius">
            <div class="row">
            {% for id, data in sponsors %}
            {% if data['type'] == '特別感謝' %}
                <div class="pic-item col-lg-4 col-md-4 col-sm-4 col-xs-6">
                    <a href="{{ data['website'] }}">
						<img src="./images/sponsor/{{ data['logo'] }}" alt="">
					</a>
                </div>
            {% endif %}
            {% endfor%}
            
            </div>
        </div>
    </div>
</section>
{% endblock %}